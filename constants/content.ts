export const TITLE = (value: string) => `# ${value}`;
export const SUBTITLE = (value: string) => `## ${value}`;
export const PARAGRAPH = (value: string) => value;
export const CAPITAL_PARAGRAPH = (value: string) => value.toUpperCase();
export const TINY_PARAGRAPH = (value: string) => value.toLowerCase();
